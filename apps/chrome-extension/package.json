{
  "name": "chrome-extension",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build && node scripts/build-extension.mjs",
    "preview": "vite preview",
    "pack": "npm run build && node scripts/pack-extension.mjs"
  },
  "devDependencies": {
    "@types/chrome": "^0.2.0",
    "typescript": "~6.0.2",
    "vite": "^8.1.0"
  }
}
